// search模块的小仓库

// state: 仓库存储数据的地方
const state = {
};

// actions: 处理action,书写自己的业务逻辑，处理异步
const actions = {
    // 书写业务逻辑，但是不能修改state
    
};

// mutations：修改state的唯一手段
const mutations = {

};

// getters: 计算属性，简化仓库数据
const getters = {}


export default {
    state,
    mutations,
    actions,
    getters
}